# CF_API_TOKEN è un token di autenticazione utilizzato per interagire con l'API di Cloudflare.
# Questo token è necessario per ottenere il certificato TLS tramite ACME.
CF_API_TOKEN=zxcvbnm-lkjhgfdsaqwertyuiop123

# PRODUCTION è una variabile booleana che indica se l'applicazione è in esecuzione in ambiente di produzione.
# Commentare se in modalità sviluppo. Quando in production, Caddy (webserver) cercherà di ottenere un
# certificato TLS per il dominio specificato nel Caddyfile.
#PRODUCTION=1