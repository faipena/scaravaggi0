# POSTGRES_DB è il nome del database PostgreSQL a cui l'applicazione si connette.
# Questo nome viene utilizzato per identificare il database all'interno del server PostgreSQL.
POSTGRES_DB=godzillaz

# POSTGRES_USER è il nome utente utilizzato per autenticarsi al database PostgreSQL.
# Questo utente deve avere i permessi necessari per accedere e gestire il database specificato.
POSTGRES_USER=dbuser

# POSTGRES_PASSWORD è la password associata all'utente PostgreSQL.
POSTGRES_PASSWORD=R4nd0mP@ssw0rd

# POSTGRES_HOSTNAME è il nome host o l'indirizzo del server PostgreSQL.
# NON MODIFICARE
POSTGRES_HOSTNAME=db

# POSTGRES_PORT è la porta su cui il server PostgreSQL è in ascolto.
# La porta predefinita per PostgreSQL è 5432.
POSTGRES_PORT=5432